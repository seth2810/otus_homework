logger:
  level: info
  file: /dev/stdout

storage:
  type: sql
  database:
    host: localhost
    port: 5432
    user: calendar
    password: calendar
    db: calendar

server:
  host: localhost
  port: 8080
